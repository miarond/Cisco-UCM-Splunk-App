import ta_cisco_ucm_realtime_monitoring_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_cisco_ucm_realtime_monitoring as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_cisco_ucm_realtime_monitoring.py
'''
class ModInputcisco_ucm_realtime_monitoring(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputcisco_ucm_realtime_monitoring, self).__init__("ta_cisco_ucm_realtime_monitoring", "cisco_ucm_realtime_monitoring", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputcisco_ucm_realtime_monitoring, self).get_scheme()
        scheme.title = ("Cisco UCM Realtime Monitoring")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("cucm_ip_or_hostname", title="Cisco UCM IP or Hostname",
                                         description="Enter the IP Address or Hostname of the Cisco UCM server.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("verify_ssl_certificate", title="Verify SSL Certificate",
                                         description="Leave unchecked if the Splunk server does not trust the Cisco UCM server\'s SSL certificate (i.e. self-signed certificates)",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("cucm_api_account", title="Cisco UCM API Account",
                                         description="Select the Cisco UCM Performance Monitoring API account.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("list_of_perfmon_counters", title="List of PerfMon API Counters",
                                         description="Enter a comma-separated list of Performance Monitoring counters to query (format: <object_name>\<counter_name>)",
                                         required_on_create=True,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-cisco-ucm-realtime-monitoring"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        account_fields.append("cucm_api_account")
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("verify_ssl_certificate")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputcisco_ucm_realtime_monitoring().run(sys.argv)
    sys.exit(exitcode)
